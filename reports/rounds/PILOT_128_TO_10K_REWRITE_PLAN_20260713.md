# 128 卡初筛报告重写计划 · 20260713

## 最终目的

把 128 卡实验组织成万卡训练前的初筛证据链，回答：

1. 主体卡体质能否支撑下一阶段扩容；
2. fail-slow 已观察到什么、万卡会怎样放大、当前还缺哪些量；
3. 哪些 collective / 并行路径最可能先限制效率；
4. 次级元件和 launch 指标应怎样作为模型敏感性与诊断信息进入报告；
5. 128→512→2048→10000 的实验门禁是什么。

## 证据层级

- **观测**：本批原始数据或报告中可直接复算的数值；
- **机制证据**：受控注入、跨节点台阶等能证明传播方式的数据；
- **规模预期**：基于极值、同步和拓扑层级提出的方向性判断；
- **未知项**：没有运行时长、规模点或恢复闭环支撑的内容。

## 正文图

1. 零事件样本量与坏卡率 95% 单侧上界；
2. 稳定性能字段相对本侧中位分布；
3. HBM host×device 相对中位热图（复用）；
4. fail-slow 独立抖动 / 真实训练 / 网络贡献三层链路；
5. 延迟注入下表面 rank gap 与全局 step 的不同响应；
6. HCCL 256MB collective 保持率（复用）；
7. Dense TP/PP MFU 相对效率（复用）；
8. launch 三类延迟的 host 相对本侧中位分布。

## 实施步骤

1. 核验原始 JSONL、CSV、JSON 与既有 SVG；
2. 使用 `reports/plot_style.py` 新增统一出图脚本并交付 SVG；
3. 重写 `PILOT_128_TO_10K_TRAINING_OUTLOOK_20260713.md`；
4. 运行脚本、检查数据数字、图片路径、Markdown 链接与 lint；
5. 不修改旧 `CAMPAIGN_FINAL` / `WITHIN_CLUSTER`，仅将其作为索引和附录来源。
